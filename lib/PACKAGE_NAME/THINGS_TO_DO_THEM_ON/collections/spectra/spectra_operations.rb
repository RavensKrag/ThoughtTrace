class Spectrum
	class << self
		def map_interval(one_axis, another_axis, input)
			# input should lie between the start and the end of one_axis
			# take that value, and re-map it to another_axis
			# 
			# Assuming linear interpolation
			
			
			# alternatively, return a lambda which compute the interpolation,
			# and don't take the "input" parameter
			
			
			# not sure if this should operate on Spectrum objects, or range objects, or something
		end
	end
end