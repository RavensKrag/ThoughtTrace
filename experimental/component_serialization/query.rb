style
callbacks

defaults
(so you can mark a query on one session, and remove it in another)



def pack
	# retrieve the serialization ID for the style object used with the Query
	
	# serialize the name of the callback class
		# not sure if callback instances have data that needs to be serialized or not...
end


class << self
	def unpack
		
	end
end